{
	"version": "2.0.0",
	"tasks": [
        {
            "type": "shell",
            "label": "Run python file",
            "command": "python",
            "args": ["${file}"],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": []
        },
    ]
}